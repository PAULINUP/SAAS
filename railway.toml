[build]
builder = "NIXPACKS"

[build.env]
NIXPACKS_STACK = "ubuntu-22"
NIXPACKS_PYTHON_VERSION = "3.11"
NIXPACKS_PATH = "."          # usa a raiz do repo

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
